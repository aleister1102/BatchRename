<Window x:Class="BatchRename.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BatchRename"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="1000"
        Loaded="Window_Loaded" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <local:PathToNameConverter x:Key="PathToNameConverter" />

        <Style x:Key="BrowseButton">
            <Setter Property="Button.Width" Value="220" />
            <Setter Property="Button.Height" Value="50" />
            <Setter Property="Button.Margin" Value="0 10" />
            <Setter Property="Button.Background" Value="#7de5ed" />
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Width" Value="220" />
            <Setter Property="Margin" Value="0 10" />
        </Style>

        <Style TargetType="ListView">
            <Setter Property="Width" Value="240" />
            <Setter Property="Height" Value="280" />
            <Setter Property="Margin" Value="0 10" />
        </Style>

        <Style x:Key="RuleGrid">
            <Setter Property="Grid.Background" Value="#4cc2ff" />
            <Setter Property="Grid.Width" Value="210" />
        </Style>

        <Style x:Key="RuleButton">
            <Setter Property="Button.FontSize" Value="12"></Setter>
            <Setter Property="Button.Background" Value="LightGray"></Setter>
            <Setter Property="Button.VerticalAlignment" Value="Center"></Setter>
        </Style>

        <Style x:Key="RuleName">
            <Setter Property="TextBlock.Margin" Value="10 0" />
            <Setter Property="TextBlock.Foreground" Value="#fff" />
            <Setter Property="TextBlock.VerticalAlignment" Value="Center" />
        </Style>
    </Window.Resources>

    <StackPanel Orientation="Horizontal">
        <DockPanel Name="ConfigPanel">
            <Button Name="BrowsePresets"
                    Content="Load preset ..." DockPanel.Dock="Top" Click="BrowsePresets_Click"
                    Style="{StaticResource BrowseButton}" />

            <ComboBox Name="PresetComboBox" DockPanel.Dock="Top"
                  SelectionChanged="PresetComboBox_SelectionChanged">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Path=., Converter={StaticResource PathToNameConverter}}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <StackPanel>
                <ListView Name="RuleListView">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid DataContext="{Binding}" Style="{StaticResource RuleGrid}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="180" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="20" />
                                </Grid.RowDefinitions>

                                <TextBlock Name="RuleTextBlock" Text="{Binding Name}" Grid.Column="0" Style="{StaticResource RuleName}" />
                                <Button Name="ActivateButton" Content="+"  Grid.Column="1" Click="ActivateButton_Click" Style="{StaticResource RuleButton}" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>

            <StackPanel>
                <ListView Name="ActiveRuleListView">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Background="#4cc2ff" Width="210" DataContext="{Binding}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="180" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="20" />
                                </Grid.RowDefinitions>

                                <Button Name="DeactivateButton" Content="-" Grid.Column="0" Click="DeactivateButton_Click" Style="{StaticResource RuleButton}" />
                                <TextBlock Name="RuleTextBlock" Text="{Binding Name}" Grid.Column="1" Style="{StaticResource RuleName}" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </DockPanel>

        <StackPanel Name="FilePanel"
                    Margin="20 0">
            <Button Name="BrowseButton"
                    Content="Browse files ..." Click="BrowseButton_Click"
                    Style="{StaticResource BrowseButton}" />

            <StackPanel Orientation="Horizontal" DockPanel.Dock="Bottom">
                <ListView Name="FileListView">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}"></TextBlock>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <ListView Name="PreviewListView">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}"></TextBlock>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </StackPanel>
    </StackPanel>
</Window>